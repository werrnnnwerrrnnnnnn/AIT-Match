<!DOCTYPE html>
<html>
<head>
  <title>About Us</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    .content-container {
      margin: 0;
      margin-top: 80px;
      padding: 0;
      flex: 1; /* Allows content to grow and push footer down */
      margin-bottom: 80px; /* Match footer height */
    }

    .image-section {
      text-align: center;
      margin: 0;
      padding: 0;
      position: relative;
    }

    .image-section img {
      width: 90%;
      height: auto;
      margin: 0;
      padding: 0;
      opacity: 0;
      animation: fadeDown 1.5s ease forwards;
      animation-delay: 0.5s;
    }

    @keyframes fadeDown {
      0% {
        opacity: 0;
        transform: translateY(-20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Change Background Color of Content Section */
    .content-section {
      padding: 20px 200px;
      margin-top: 0px;
      text-align: left;
      position: relative;
      z-index: 1;
      background-color: none;
      border-radius: 10px;
    }

    /* Ribbon Animation */
    .ribbon-container {
      display: flex;
      justify-content: center;
      margin: 20px 0;
      position: relative;
      z-index: 0;
    }

    .ribbon {
      position: relative;
      padding: 10px 30px;
      background-color: #DF9EB2;
      color: white;
      font-size: 2em;
      font-weight: bold;
      text-transform: uppercase;
      text-align: center;
      clip-path: polygon(10% 0, 90% 0, 100% 50%, 90% 100%, 10% 100%, 0 50%);
    }

    /* Text Container Styling */
    .text-container {
      width: 60%;%
      margin-top: 20px;
      margin: 0 auto;
      padding: 15px;
      background-color: white;
      border-radius: 8px;
      position: relative;
      z-index: 1; /* Ensure it’s below the footer */
    }

    .text-container p {
      font-size: 1.2em;
      line-height: 1.8;
      margin: 0;
    }

    .read-more {
      display: none;
      margin-top: 10px;
    }

    .btn-get-started {
      display: inline-block;
      background-color: transparent;
      color: #617952;
      padding: 15px 30px;
      font-size: 16px;
      text-decoration: none;
      border: 2px solid #617952;
      font-weight: bold;
      border-radius: 50px;
      width: 150px;
      text-align: center;
      transition: transform 0.3s ease, background-color 0.3s ease, color 0.3s ease, box-shadow 0.3s ease;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.2);
      margin: 20px auto;
      display: block;
      position: relative;
      z-index: 1;
    }

    .btn-get-started:hover {
      transform: translateY(-5px);
      background-color: #FDEDF1;
      color: #617952;
      box-shadow: 0px 6px 8px rgba(0, 0, 0, 0.2);
    }
  </style>
  <script>
    function toggleReadMore() {
      const readMoreContent = document.getElementById('read-more-content');
      const readMoreButton = document.getElementById('read-more-button');
      if (readMoreContent.style.display === 'none' || readMoreContent.style.display === '') {
        readMoreContent.style.display = 'block';
        readMoreButton.innerText = 'Read Less';
      } else {
        readMoreContent.style.display = 'none';
        readMoreButton.innerText = 'Read More';
      }
    }
  </script>
</head>
<body>

<div class="content-container">
  <!-- Image Section -->
  <div class="image-section">
    <img src="<%= asset_path('about-page.png') %>" alt="Story Image">
  </div>

  <!-- Content Section -->
  <div class="content-section">
    <!-- Animated Ribbon Title -->
    <div class="ribbon-container">
      <div class="ribbon">About Us</div>
    </div>

    <!-- Text Container -->
    <div class="text-container">
      <p>
        〰 AIT Match connects AIT students in a safe, authentic, and engaging way. 
          Find like-minded peers based on academic programs, majors, schools, and interests. 
          Our platform ensures safety with university-only login, admin oversight, and reporting features. 
          Once matched, enjoy real-time chats with connections who’ve mutually accepted. 
      </p> <br>
      <p>
            〰 AIT Match — your gateway to meaningful connections at AIT.
      </p>
      </div>
    </div>
  </div>
</div>

</body>
</html>