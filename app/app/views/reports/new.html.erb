<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Report Profile | AIT Match</title>
  
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #FDEDF1;
      margin: 0;
      padding: 0;
    }

    .report-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 30px;
      background-color: #FDEDF1;
      margin-bottom: 60px;
      margin-top: 60px;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
      gap: 15px;
    }

    .report-card {
      background-color: #fff;
      padding: 35px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 100%;
    }

    .report-header {
      width: 80%;
      max-width: 300px;
      display: block;
      margin-top: 70px; /* Increased top margin */
      margin-bottom: 20px;
      position: relative;
      cursor: pointer;
      transition: transform 0.2s ease-in-out;
    }

    .report-header:hover {
      animation: wiggle 0.9s infinite;
    }

    @keyframes wiggle {
        0% { transform: translate(0, 0) rotate(0deg); }
        10% { transform: translate(-1px, 1px) rotate(-2deg); }
        20% { transform: translate(1px, -1px) rotate(2deg); }
        30% { transform: translate(-1px, -1px) rotate(-2deg); }
        40% { transform: translate(1px, 1px) rotate(2deg); }
        50% { transform: translate(-1px, 1px) rotate(-2deg); }
        60% { transform: translate(1px, -1px) rotate(2deg); }
        70% { transform: translate(-1px, -1px) rotate(-2deg); }
        80% { transform: translate(1px, 1px) rotate(2deg); }
        90% { transform: translate(-1px, 1px) rotate(-2deg); }
        100% { transform: translate(0, 0) rotate(0deg); }
    }

    .report-form-group {
      margin-bottom: 10px;
    }

    .report-form-group label {
      display: block;
      font-weight: bold;
      margin-bottom: 4px;
      color: #333;
      font-size: 0.9rem;
    }

    .form-control {
      width: 100%;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 14px;
    }

    .report-submit-container {
      text-align: center;
      margin-top: 15px;
    }

    .report-btn {
      background-color: #F7DDE3;
      color: #617952;
      padding: 8px 16px;
      border: none;
      border-radius: 18px;
      cursor: pointer;
      text-align: center;
      display: inline-block;
      transition: transform 0.3s ease, background-color 0.3s ease, color 0.3s ease, box-shadow 0.3s ease;
      box-shadow: 0px 3px 5px rgba(0, 0, 0, 0.2);
      font-size: 0.9rem;
    }

    .report-btn:hover {
      background-color: #617952;
      color: #F7DDE3;
      box-shadow: 0px 5px 7px rgba(0, 0, 0, 0.2);
      transform: translateY(-3px);
    }
  </style>
</head>
<body>

<div class="report-container">
  <%= image_tag 'report-heading.png', alt: 'Report a Profile', class: 'report-header', style: 'width:100%; max-width:250px; display:block; margin:0 auto;' %>

    <div class="report-card">
      <h1 style="font-size: 1.2rem;">‼️ Report <%= @reported_profile&.user_name || "Profile Not Found" %> ‼️</h1>
      
      <%= form_with(model: @report, url: reports_path(reported_profile_id: @reported_profile.id), local: true) do |f| %>
        <div class="report-form-group">
          <%= f.label :reason, "Reason for reporting" %><br>
          <%= f.text_area :reason, placeholder: "Describe why you're reporting this profile", class: "form-control" %>
        </div>
        
        <div class="report-submit-container">
          <%= f.submit "Submit Report", class: "report-btn" %>
        </div>
      <% end %>
    </div>
  </div>

</body>
</html>