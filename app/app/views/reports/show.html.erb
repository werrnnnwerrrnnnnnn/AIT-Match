<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Report Details</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #FDEDF1;
      margin: 0;
      padding: 0;
    }

    .report-details-container {
      display: flex;
      flex-direction: column;
      align-items: center; /* Center align the content */
      padding: 50px;
      background-color: #FDEDF1;
      margin-top: 40px;
      margin-bottom: 100px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
      gap: 20px;
    }

    .report-details-card {
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 100%;
    }

    .report-details-card h1 {
      color: #617952;
      text-align: center;
    }

    .report-details-card p {
      margin: 10px 0;
      font-size: 16px;
      color: #333;
    }

    .back-link-container {
      text-align: center;
      margin-top: 20px;
    }

    .back-link {
      background-color: #F7DDE3;
      color: #617952;
      padding: 10px 20px;
      border-radius: 20px;
      text-decoration: none;
      display: inline-block;
      font-weight: bold;
      transition: transform 0.3s ease, background-color 0.3s ease, box-shadow 0.3s ease;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.2);
    }

    .back-link:hover {
      background-color: #FBCAD0;
      transform: translateY(-5px);
      box-shadow: 0px 6px 8px rgba(0, 0, 0, 0.2);
    }
  </style>
</head>
<body>

  <div class="report-details-container">
    <div class="report-details-card">
      <h1>Report Details</h1>

      <p><strong>Reported User:</strong> <%= link_to @report.reported_profile.user_name, profile_path(@report.reported_profile) %></p>
      <p><strong>Reason:</strong> <%= @report.reason %></p>
      <p><strong>Status:</strong> <%= @report.status %></p>
      <p><strong>Submitted On:</strong> <%= @report.created_at.in_time_zone("Asia/Bangkok").strftime("%d %B %Y, %I:%M %p") %></p>
      
      <!-- Center-aligned Back to My Reports button inside the report-details-card -->
      <div class="back-link-container">
        <%= link_to 'Back to My Reports', reports_path, class: 'back-link' %>
      </div>
    </div>
  </div>

</body>
</html>